  <h1> Albums for {{albums.0.artists.0.name}}</h1> <!-- array, os 0 sao para limitar quais queremos-->
<ul> 
  {{#each albums}}
  <li> 
      {{name}}
      <img src={{images.0.url}}>
  </li>
 
    <a href="/tracks/{{id}}">View tracks</a> <!-- this is ID now is from album -->
  {{/each}}
  
</ul>